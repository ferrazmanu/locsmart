const CAMERAS = "cameras";
const CAMERA = "camera";

const COMPANIES = "companies";
const COMPANY = "company";

const GROUPS = "groups";
const GROUP = "group";

const USERS = "users";
const USER = "user";

export const queryKey = {
  CAMERAS,
  CAMERA,
  COMPANIES,
  COMPANY,
  GROUP,
  GROUPS,
  USER,
  USERS,
};
